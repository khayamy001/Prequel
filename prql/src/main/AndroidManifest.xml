<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.torchbyte.prql.pager">

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/Theme.BBQ">
        <activity
                android:name="com.torchbyte.prql.pager.activity.ViewPagerActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name="com.torchbyte.prql.pager.activity.DetailActivity"
                android:label="@string/activity_pager"
                android:theme="@style/Theme.BBQ"/>

        <activity
                android:name="com.torchbyte.prql.pager.activity.SlideshowActivity"
                android:label="@string/activity_slideshow"
                android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
                android:configChanges="orientation|screenSize"/>
    </application>

</manifest>
